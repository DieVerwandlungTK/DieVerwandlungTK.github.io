{
  "name": "water-research-homepage",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "tsx --test tests/*.test.ts",
    "test:browser": "playwright test"
  },
  "dependencies": { "vgpu": "0.4.0" },
  "devDependencies": { "@webgpu/types": "^0.1.69", "typescript": "^5.9.3", "vite": "^7.3.0", "tsx": "^4.21.0", "@playwright/test": "^1.58.0" }
}
